 const AuthReducer = (state = {}, action) => {
	switch (action.type) {
		case 'LOGGEDIN':
		
			break;
		case 'LOGGEDOUT':
			break;
		default:
			break;
	}

	return state;
};

export default AuthReducer;// JavaScript Document